[pytest]
asyncio_mode = auto
testpaths = backend/tests
pythonpath = backend
filterwarnings =
    ignore::DeprecationWarning

[coverage:run]
source = app
branch = false
omit =
    */app/data/*
    */__pycache__/*

[coverage:report]
show_missing = true
skip_covered = false
